#!/usr/bin/php
<?php
    if ($argv != 2 || ($argv[1] != "moyenne" && $argv[1] != "moyenne_user" && $argv[1] != "ecart_moulinette"))
        return ;
    if ($argv[1] == "moyenne")

    // faire une array key(name):array(notes)
?>